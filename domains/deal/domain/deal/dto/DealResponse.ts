export interface DealResponse {
    id: number
    title: string
    description: string
    currentPrice: number
    deadline: string
    images: string[]
    createdAt: string
    hashtags: string[]
}
